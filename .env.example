# TODO: Add your Firebase credentials from Firebase Console:
# Project settings → General → Your apps → SDK setup and configuration
# Copy this to .env and fill in the values (no commas after values).
#
# For Phone Auth: Enable it in Firebase Console → Authentication → Sign-in method → Phone.
# Add 127.0.0.1 to Authorized domains (Authentication → Settings) and open the app at
# http://127.0.0.1:5173 — localhost is not allowed for phone auth.

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_DATABASE_URL=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
